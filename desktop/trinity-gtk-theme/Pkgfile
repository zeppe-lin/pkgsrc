# Description:  Trinity GTK+2 and GTK+3 theme
# URL:          https://zeppe-lin.github.io/
# Depends on:   gtk-engines

name=trinity-gtk-theme
version=0.1
release=1
source=/dev/null

build() {
	mkdir -p $PKG/usr/share/themes/trinity
	cp -Pr $PKGMK_ROOT/src/* $PKG/usr/share/themes/trinity/

	# remove junk
	cd $PKG/usr/share/themes/trinity
	rm COPYING COPYRIGHT gen-gtk.gresource.xml.sh Makefile
	rm README TODO
}
